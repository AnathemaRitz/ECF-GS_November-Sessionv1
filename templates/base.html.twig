<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="robots" content="noindex, noarchive, noimageindex, nosnippet">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Bienvenue sur le site de Gamestore! {% endblock %}</title>
        <link rel="icon" href="{{ asset("images/favicon.png") }}"><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
            <link rel="stylesheet" href="{{ asset('styles/carousel.css') }}">
        {% endblock %}
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
                <div class="container-fluid">
                    <a class="navbar-brand" href="/">
                        <img src="{{ asset('images/logo_lateral.png')}}" width="250" height="75"  class="d-inline-block align-top" alt="Logo de Gamestore">
                        {#GAMESTORE#}
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item align-content-start">
                                <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                            </li>
                            <li class="nav-item align-content-start">
                                <a class="nav-link" href="{{ path('app_shop') }}">E-Shop</a>
                            </li>
                          {#  <li class="nav-item dropdown align-content-start">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Genres
                                </a>
                                <ul class="dropdown-menu">
                                    {%for genre in allGenres %}
                                        <li><a class="dropdown-item" href="{{ path('app_genre', {slug : genre.slug}) }}">{{ genre.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            </li>#}
                            <li class="nav-item dropdown align-content-start">
                                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        Nos Magasins
                                    </a>
                               <ul class="dropdown-menu">
                                   {%for store in allStores %}
                                    <li><a class="dropdown-item" href="{{ path('app_store', {slug : store.slug}) }}">{{ store.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            </li>
                            <li class="nav-item dropdown align-content-end">
                                {% if app.user%}
                                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        {{ app.user.firstname }}
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" href="{{ path('app_account') }}">Mon espace</a></li>
                                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
                                    </ul>
                                {% else %}
                                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        Compte
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" href="{{ path('app_login') }}">Me connecter</a></li>
                                        <li><a class="dropdown-item" href="{{ path('app_register') }}">Créer un compte</a></li>
                                    </ul>
                                {% endif %}
                                {#<ul class="dropdown-menu">
                                    <li><a class="dropdown-item" href="{{ path('app_register') }}">Créer un compte</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_account') }}">Mon espace</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_login') }}">Me connecter</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
                                </ul>#}
                            </li>
                                <li class="nav-item align-content-end ">
                                    <a class="nav-link" href="{{ path('app_cart') }}">
                                        <i data-feather="shopping-cart"></i>Panier
                                        <span class="badge text-bg-light">{{ fullCartQuantity }}</span>
                                    </a>
                                </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </header>

    </head>
    <body>
<main>
    <div class="container">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert mt-4 alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
    {% block body %}
    {% endblock %}

</main>
        <!-- FOOTER -->
        <footer class="container">
            <p class="float-end"><a href="#">Retour en haut de page</a></p>
            <p>&copy; 2021-2024 Gamestore. &middot; <a href="#">Politique de confidentialité</a> &middot; <a href="#">Mentions légales</a></p>
            <br>
            <p class="justify-content-center"><I>Ceci est un site vitrine fictif pour une entreprise fictive, créé dans le cadre d'une formation.</I></p>

        </footer>



    <script src="{{ asset('app.js') }}"></script>


    </body>


</html>
