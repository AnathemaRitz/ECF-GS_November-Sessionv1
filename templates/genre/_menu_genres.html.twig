<ul class="nav flex-column bg-info">
    {% for genre in allGenres %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_genre', { slug : genre.slug } ) }}">{{ genre.name }}</a>
        </li>
    {% endfor %}
</ul>